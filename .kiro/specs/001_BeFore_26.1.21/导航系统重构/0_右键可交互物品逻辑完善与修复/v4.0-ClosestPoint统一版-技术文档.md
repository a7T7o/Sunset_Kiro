# v4.0 ClosestPoint 统一版 - 技术文档

**创建日期**: 2026-01-19  
**版本**: v4.0  
**状态**: 待验收

---

## 一、核心设计思想

### 问题背景

旧方案使用"Collider 底部中心"作为目标锚点，导致：
1. 从上方接近时绕路（玩家在箱子上方，却要绕到下方才能交互）
2. 导航系统和交互系统使用不同的距离计算方式，导致"导航到了但交互失败"

### 解决方案

**统一使用 `Collider.ClosestPoint(playerPos)` 计算距离**

- **语义正确**："距离"就是"到物体的最近距离"
- **路径最短**：从任何方向接近都走到最近的边缘点
- **统一简洁**：导航、停止、交互全部使用同一个距离计算方式

---

## 二、核心代码

### 2.1 GameInputManager - 距离计算

```csharp
/// <summary>
/// 🔥 v4.0：获取目标最近点（使用 ClosestPoint）
/// 
/// 核心思路：
/// 1. 使用 Collider.ClosestPoint(playerPos) 计算玩家到目标的最近点
/// 2. 这样从任何方向接近都是最短路径，不会绕路
/// 3. 与 PlayerAutoNavigator 使用相同的距离计算方式
/// </summary>
private Vector2 GetTargetAnchor(Transform target, Vector2 playerPos)
{
    // 尝试获取 Collider
    var collider = target.GetComponent<Collider2D>();
    if (collider == null)
        collider = target.GetComponentInChildren<Collider2D>();
    
    if (collider != null)
    {
        // 🔥 使用 ClosestPoint 计算玩家到 Collider 的最近点
        return collider.ClosestPoint(playerPos);
    }
    
    return target.position;
}
```

### 2.2 GameInputManager - 交互处理

```csharp
/// <summary>
/// 🔥 v4.0：统一处理可交互物体
/// 使用 ClosestPoint 计算距离，确保从任何方向接近都是最短路径
/// </summary>
private void HandleInteractable(IInteractable interactable, Transform target, Vector2 playerCenter)
{
    // 导航开始前取消 Held 状态
    var manager = InventoryInteractionManager.Instance;
    if (manager != null && manager.IsHolding)
    {
        manager.Cancel();
    }
    
    // 🔥 v4.0：使用 ClosestPoint 计算玩家到目标的最近距离
    Vector2 targetPos = GetTargetAnchor(target, playerCenter);
    float distance = Vector2.Distance(playerCenter, targetPos);
    float interactDist = interactable.InteractionDistance;
    
    if (distance <= interactDist)
    {
        // 在交互距离内，直接交互
        TryInteract(interactable);
    }
    else
    {
        // 距离太远，导航到目标附近后交互
        if (autoNavigator != null)
        {
            autoNavigator.ForceCancel();
            
            autoNavigator.FollowTarget(target, interactDist * 0.8f, () =>
            {
                // 到达后距离复核
                TryInteractWithDistanceCheck(interactable, target);
            });
        }
    }
}
```

### 2.3 GameInputManager - 距离复核

```csharp
/// <summary>
/// 🔥 v4.0：带距离复核的交互（使用 ClosestPoint）
/// </summary>
private void TryInteractWithDistanceCheck(IInteractable interactable, Transform target)
{
    if (interactable == null || target == null) return;
    
    // 获取玩家位置
    Vector2 playerPos = GetPlayerCenter();
    
    // 🔥 v4.0：使用 ClosestPoint 计算距离
    Vector2 targetPos = GetTargetAnchor(target, playerPos);
    float distance = Vector2.Distance(playerPos, targetPos);
    float interactDist = interactable.InteractionDistance;
    
    // 允许 20% 容差
    if (distance > interactDist * 1.2f)
    {
        LogWarningOnce("DistanceTooFar", $"[GameInputManager] 距离过远，取消交互: {distance:F2} > {interactDist * 1.2f:F2}");
        return;
    }
    
    TryInteract(interactable);
}
```

### 2.4 PlayerAutoNavigator - 目标点计算

```csharp
// 缓存目标 Collider，避免每帧 GetComponent
private Collider2D _cachedTargetCollider;
private Transform _cachedTargetTransform;

/// <summary>
/// 🔥 v4.0：获取目标最近点（使用 ClosestPoint）
/// 
/// 核心思路：
/// 1. 使用 Collider.ClosestPoint(playerPos) 计算玩家到目标的最近点
/// 2. 这样从任何方向接近都是最短路径，不会绕路
/// 3. 与 GameInputManager 使用相同的距离计算方式
/// </summary>
private Vector2 GetTargetAnchorPoint(Transform target)
{
    if (target == null) return transform.position;
    
    // 缓存 Collider
    if (_cachedTargetTransform != target)
    {
        _cachedTargetTransform = target;
        _cachedTargetCollider = target.GetComponent<Collider2D>();
        if (_cachedTargetCollider == null)
            _cachedTargetCollider = target.GetComponentInChildren<Collider2D>();
    }
    
    if (_cachedTargetCollider != null)
    {
        // 🔥 v4.0：使用 ClosestPoint 计算玩家到 Collider 的最近点
        Vector2 playerPos = GetPlayerPosition();
        return _cachedTargetCollider.ClosestPoint(playerPos);
    }
    
    return target.position;
}
```

### 2.5 PlayerAutoNavigator - 停止距离检查

```csharp
/// <summary>
/// 🔥 v4.0：检查玩家是否足够近可以与目标交互
/// 
/// 核心思路：
/// 1. 使用与 GameInputManager 相同的距离计算方式
/// 2. 玩家中心 → Collider 最近点（ClosestPoint）
/// </summary>
private bool IsCloseEnoughToInteract(Vector2 playerPos)
{
    Vector2 targetAnchor = GetTargetAnchorPoint(targetTransform);
    float distance = Vector2.Distance(playerPos, targetAnchor);
    return distance <= followStopRadius;
}
```

### 2.6 PlayerAutoNavigator - 导航日志

```csharp
// FollowTarget 开始时
Vector2 playerStart = GetPlayerPosition();
Vector2 targetClosest = GetTargetAnchorPoint(t);
float distToTarget = Vector2.Distance(playerStart, targetClosest);

var collider = _cachedTargetCollider;
if (collider != null)
{
    Bounds bounds = collider.bounds;
    Debug.Log($"<color=green>[Nav] 开始导航: 目标={t.name}</color>\n" +
              $"  玩家起点: {playerStart}\n" +
              $"  目标最近点(ClosestPoint): {targetClosest}, Collider大小: {(Vector2)bounds.size}\n" +
              $"  当前距离: {distToTarget:F2}, 停止半径: {followStopRadius:F2}");
}

// CompleteArrival 时
Vector2 playerFinal = GetPlayerPosition();
Vector2 targetClosest = GetTargetAnchorPoint(targetTransform);
float distToTarget = Vector2.Distance(playerFinal, targetClosest);

Debug.Log($"<color=cyan>[Nav] 导航完成: 目标={targetName}</color>\n" +
          $"  玩家最终位置: {playerFinal}\n" +
          $"  到目标最近点距离: {distToTarget:F2}\n" +
          $"  停止半径: {followStopRadius:F2}");
```

---

## 三、流程图

### 3.1 右键点击可交互物体流程

```
用户右键点击
    ↓
GameInputManager.HandleRightClickAutoNav()
    ↓
检测点击位置的 IInteractable
    ↓
HandleInteractable(interactable, target, playerCenter)
    ↓
计算距离: GetTargetAnchor(target, playerCenter) → ClosestPoint
    ↓
┌─────────────────────────────────────────┐
│ distance <= interactDist?               │
├─────────────────────────────────────────┤
│ YES → TryInteract() 直接交互            │
│ NO  → FollowTarget() 导航后交互         │
└─────────────────────────────────────────┘
```

### 3.2 导航到目标流程

```
FollowTarget(target, stopRadius, onArrived)
    ↓
ForceCancel() 取消旧导航
    ↓
计算目标点: CalculateClosestApproachPoint() → ClosestPoint
    ↓
计算停止半径: CalculateOptimalStopRadius()
    ↓
BuildPath() 构建路径
    ↓
Update() 每帧执行
    ↓
ExecuteNavigation()
    ↓
┌─────────────────────────────────────────┐
│ IsCloseEnoughToInteract()?              │
│ (使用 ClosestPoint 计算距离)            │
├─────────────────────────────────────────┤
│ YES → CompleteArrival() 执行回调        │
│ NO  → 继续移动                          │
└─────────────────────────────────────────┘
```

---

## 四、参数说明

### 4.1 GameInputManager 参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `interactableMask` | LayerMask | - | 可交互物体的 Layer |
| `interactableTags` | string[] | - | 可交互物体的 Tag |
| `navClickDeadzone` | float | 0.3 | 玩家周围的点击死区 |
| `navClickCooldown` | float | 0.15 | 导航点击间隔（防抖） |
| `minNavDistance` | float | 0.5 | 最小导航距离 |

### 4.2 PlayerAutoNavigator 参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `stopDistance` | float | 0.2 | 普通导航停止距离 |
| `waypointTolerance` | float | 0.15 | 路径点容差 |
| `destinationChangeThreshold` | float | 0.3 | 目标点去抖阈值 |
| `enableLineOfSightOptimization` | bool | true | 启用视线优化 |
| `losSafetyMargin` | float | 0.2 | 视线检测安全边距 |

### 4.3 IInteractable 接口

| 属性/方法 | 说明 |
|----------|------|
| `InteractionDistance` | 交互距离（玩家需要在此距离内才能交互） |
| `InteractionPriority` | 交互优先级（多个目标时选择优先级高的） |
| `CanInteract(context)` | 检查是否可以交互 |
| `OnInteract(context)` | 执行交互 |

---

## 五、复现/回滚指南

### 5.1 如果需要回滚到旧版本

**GameInputManager.cs**:
```csharp
// 旧版本：使用 Collider 底部中心
private Vector2 GetTargetAnchor(Transform target)
{
    var collider = target.GetComponent<Collider2D>();
    if (collider != null)
    {
        Bounds bounds = collider.bounds;
        return new Vector2(bounds.center.x, bounds.min.y);
    }
    return target.position;
}
```

**PlayerAutoNavigator.cs**:
```csharp
// 旧版本：使用 Collider 底部中心
private Vector2 GetTargetAnchorPoint(Transform target)
{
    var collider = target.GetComponent<Collider2D>();
    if (collider != null)
    {
        Bounds bounds = collider.bounds;
        return new Vector2(bounds.center.x, bounds.min.y);
    }
    return target.position;
}
```

### 5.2 关键修改点清单

| 文件 | 方法 | 修改内容 |
|------|------|---------|
| `GameInputManager.cs` | `GetTargetAnchor()` | 改为接收 `playerPos` 参数，返回 `ClosestPoint` |
| `GameInputManager.cs` | `HandleInteractable()` | 使用新的 `GetTargetAnchor()` |
| `GameInputManager.cs` | `TryInteractWithDistanceCheck()` | 使用新的 `GetTargetAnchor()` |
| `PlayerAutoNavigator.cs` | `GetTargetAnchorPoint()` | 使用 `ClosestPoint` |
| `PlayerAutoNavigator.cs` | `IsCloseEnoughToInteract()` | 使用 `ClosestPoint` |
| `PlayerAutoNavigator.cs` | `FollowTarget()` | 日志输出更新 |
| `PlayerAutoNavigator.cs` | `CompleteArrival()` | 日志输出更新 |

---

## 六、测试用例

### 6.1 基本功能测试

| 测试场景 | 预期结果 |
|---------|---------|
| 玩家在箱子上方，右键点击箱子 | 玩家直接向下走到箱子边缘，不绕路 |
| 玩家在箱子下方，右键点击箱子 | 玩家直接向上走到箱子边缘 |
| 玩家在箱子左侧，右键点击箱子 | 玩家直接向右走到箱子边缘 |
| 玩家在箱子右侧，右键点击箱子 | 玩家直接向左走到箱子边缘 |
| 玩家已在交互距离内，右键点击箱子 | 直接打开箱子 UI，不导航 |

### 6.2 边界条件测试

| 测试场景 | 预期结果 |
|---------|---------|
| 目标没有 Collider | 使用 `target.position` 作为目标点 |
| 目标 Collider 在子物体上 | 正确获取子物体的 Collider |
| 导航过程中目标被销毁 | 导航取消，不报错 |
| 导航过程中玩家按 WASD | 导航取消，玩家手动控制 |

### 6.3 日志验证

导航开始时应输出：
```
[Nav] 开始导航: 目标=Chest_01
  玩家起点: (10.5, 5.2)
  目标最近点(ClosestPoint): (12.0, 5.2), Collider大小: (1.0, 1.5)
  当前距离: 1.50, 停止半径: 1.20
```

导航完成时应输出：
```
[Nav] 导航完成: 目标=Chest_01
  玩家最终位置: (11.2, 5.2)
  到目标最近点距离: 0.80
  停止半径: 1.20
```

---

## 七、已知限制

1. **ClosestPoint 对于复杂形状的 Collider 可能不够精确**
   - 对于 PolygonCollider2D，ClosestPoint 返回的是多边形边缘上的最近点
   - 对于 CompositeCollider2D，可能返回组合后的边缘点

2. **每帧重新计算 ClosestPoint 有性能开销**
   - 已通过缓存 Collider 引用优化
   - 对于大量同时导航的场景可能需要进一步优化

3. **停止半径计算依赖 IInteractable.InteractionDistance**
   - 如果目标没有实现 IInteractable，使用默认值

---

**文档维护者**: Kiro  
**最后更新**: 2026-01-19
