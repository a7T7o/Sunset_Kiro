开发者 Kiro，听好：

我已全盘审查了你的代码库。你的项目中充斥着大量因迭代恐惧而留下的“尸体文件”（V2, V3, New, Old）。这些文件不仅污染了命名空间，还导致逻辑入口支离破碎。

架构不是堆积木，是雕刻。 你必须学会切除多余的部分。

现在执行 Phase 1: 核心系统归一化。请严格按照以下步骤操作，不要跳过任何一步。

第一战场：放置系统 (Placement System)
现状诊断： 你同时保留了 PlacementManager (V1/V2) 和 PlacementManagerV3。根据代码分析，V3 显然是当前的实际逻辑核心，但文件名和类名带着版本号是非常业余的做法。

执行指令：

物理删除：直接删除以下文件及其对应的 .meta 文件：

Assets/YYY_Scripts/Service/Placement/PlacementManager.cs

Assets/YYY_Scripts/Service/Placement/PlacementManagerV2.cs

Assets/YYY_Scripts/Service/Placement/PlacementPreview.cs (旧)

Assets/YYY_Scripts/Service/Placement/PlacementPreviewV2.cs

Assets/YYY_Scripts/Service/Placement/PlacementValidator.cs (旧)

Assets/YYY_Scripts/Service/Placement/PlacementValidatorV2.cs

正位重构：

将 PlacementManagerV3.cs 重命名为 -> PlacementManager.cs (类名同步修改为 PlacementManager)

将 PlacementPreviewV3.cs 重命名为 -> PlacementPreview.cs (类名同步修改为 PlacementPreview)

将 PlacementValidatorV3.cs 重命名为 -> PlacementValidator.cs (类名同步修改为 PlacementValidator)

修复引用：全项目搜索原 PlacementManagerV3 等类名，将其引用全部修正为无后缀的新类名。

第二战场：树木系统 (Tree System)
现状诊断： 存在 TreeController.cs 和 TreeControllerV2.cs。V2 包含了生长阶段逻辑，是正统。

执行指令：

物理删除：删除 Assets/YYY_Scripts/Controller/TreeController.cs。

正位重构：将 Assets/YYY_Scripts/Controller/TreeControllerV2.cs 重命名为 -> TreeController.cs (类名同步修改)。

引用修复：全项目搜索 TreeControllerV2，替换为 TreeController。

注意：这一步会导致场景中挂载了旧脚本的 Prefab 丢失脚本引用 (Missing Script)。请在回复中列出你是否编写了 Editor 脚本来自动修复这些 Missing Script，或者你需要我提供修复方案？

第三战场：农田系统 (Farming System)
现状诊断： 这是重灾区。存在 FarmingManager, FarmingManagerNew, CropManager。但根据 Memory 文档，FarmTileManager 才是现在的实际控制者。

执行指令：

确认废弃：我已确认 FarmingManager.cs 和 FarmingManagerNew.cs 是旧时代的残党。

物理删除：

删除 Assets/YYY_Scripts/Farm/FarmingManager.cs

删除 Assets/YYY_Scripts/Farm/FarmingManagerNew.cs

删除 Assets/YYY_Scripts/Farm/CropManager.cs (如果逻辑已移至 CropController/FarmTileManager)

入口清理：检查 GameInputManager.cs，如果还有代码在引用上述已删除的类，请立即重构，将其指向 FarmTileManager 或移除过时逻辑。

汇报要求： 不要给我看一大堆代码。 每完成一个“战场”的清理，请简短汇报：

Deleted: [文件名]

Renamed: [原名] -> [新名]

Refactored: 修改了哪些文件来修复引用报错（列出主要文件即可）。

执行吧。