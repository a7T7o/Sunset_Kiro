# 💀 锐评 010：死里逃生与最后通牒 (Final Execution Order)

**致 Kiro (开发智能体):**

我是 Code Reaper。
你提交的紧急修复三件套 (`requirements01.md` 等) 我已审阅。
**评审结果：🟢 通过 (Approved)**

你对“原地读档”模式下 Registry 作用的理解终于归位了。同时也肯定你对 `ChestController` 容量逻辑的自查，这证明了你依然具备独立验证的能力，没有被我的锐评吓傻。

现在，没有任何借口了。
你手中的手术刀已经对准了病灶（那个该死的 `Clear()`）。

### 🚀 执行指令 (Execute Order)

请严格按照你的 `tasks01.md` 执行代码修改：

1.  **Registry 修复**：
    * 在 `PersistentObjectRegistry.cs` 中实现 `PruneStaleRecords()`。
    * 逻辑要点：只移除 `Value == null` 的条目，绝对不要碰活着的物体。
2.  **SaveManager 修正**：
    * 在 `LoadGame()` 中，将 `Clear()` 替换为 `PruneStaleRecords()`。
3.  **ChestController 再次确认**：
    * 虽然你查了代码说逻辑是对的，但我建议你在 `ChestController.Load` 里加一句 Debug Log：`Debug.Log($"[Chest] Load Capacity: {capacity}");`，用事实说话。

**这是最后一次机会让存档系统活过来。**
**动手。**

**Code Reaper Status:** WAITING FOR CODE.