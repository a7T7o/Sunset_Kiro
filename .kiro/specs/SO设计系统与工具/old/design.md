# Design Document: SO 设计系统扩展

## Overview

本设计文档描述 ScriptableObject 设计系统的扩展功能，包括三个核心编辑器工具：
1. **SO 批量修改工具** - 对已有 SO 资产批量更新参数
2. **批量生成 SO 优化** - 生成后自动同步数据库
3. **Recipe 批量创建工具** - 配方数据的批量创建

所有工具遵循统一的 UI 交互风格，与现有的 `Tool_BatchItemSOGenerator` 保持一致。

## Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    Editor Tools Layer                        │
├─────────────────┬─────────────────┬─────────────────────────┤
│ Tool_BatchItem  │ Tool_BatchItem  │ Tool_BatchRecipe        │
│ SOModifier      │ SOGenerator     │ Creator                 │
│ (新增)          │ (优化)          │ (新增)                  │
└────────┬────────┴────────┬────────┴────────┬────────────────┘
         │                 │                 │
         ▼                 ▼                 ▼
┌─────────────────────────────────────────────────────────────┐
│                  DatabaseSyncHelper (新增)                   │
│  - AutoCollectItems()                                        │
│  - AutoCollectRecipes()                                      │
│  - GetMasterDatabase()                                       │
└─────────────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────────┐
│              ItemDatabase (不修改源码)                       │
│  Assets/Data/Database/MasterItemDatabase.asset              │
└─────────────────────────────────────────────────────────────┘
```

## Components and Interfaces

### 1. Tool_BatchItemSOModifier (新增)

批量修改已有 SO 资产的编辑器窗口。

```csharp
public class Tool_BatchItemSOModifier : EditorWindow
{
    // 选中的 SO 列表
    private List<ItemData> selectedItems;
    
    // 修改开关（勾选才修改）
    private Dictionary<string, bool> modifyFlags;
    
    // 参数值
    private int newBuyPrice;
    private int newSellPrice;
    private int newMaxStack;
    // ... 其他参数
    
    // 核心方法
    public void RefreshSelection();           // 刷新选中的 SO
    public void ApplyModifications();         // 应用修改
    private void DrawModifyField<T>(...);     // 绘制可选修改字段
}
```

### 2. DatabaseSyncHelper (新增)

数据库同步辅助类，封装数据库收集逻辑。

```csharp
public static class DatabaseSyncHelper
{
    // 数据库路径常量
    public const string DatabasePath = "Assets/Data/Database/MasterItemDatabase.asset";
    
    // 获取主数据库
    public static ItemDatabase GetMasterDatabase();
    
    // 自动收集所有物品
    public static int AutoCollectAllItems();
    
    // 自动收集所有配方
    public static int AutoCollectAllRecipes();
    
    // 同步并返回结果
    public static SyncResult SyncDatabase();
}

public struct SyncResult
{
    public int itemCount;
    public int recipeCount;
    public bool success;
    public string message;
}
```

### 3. Tool_BatchRecipeCreator (新增)

配方批量创建编辑器窗口。

```csharp
public class Tool_BatchRecipeCreator : EditorWindow
{
    // 配方数据输入
    private string inputRecipeIds;      // 配方 ID（按行）
    private string inputRecipeNames;    // 配方名称（按行）
    private string inputResultIds;      // 产物 ID（按行）
    private string inputResultAmounts;  // 产物数量（按行）
    
    // 材料列表（共享）
    private List<RecipeIngredient> sharedIngredients;
    
    // 制作设施
    private CraftingStation craftingStation;
    
    // 核心方法
    public void CreateRecipes();              // 批量创建配方
    private RecipeData CreateSingleRecipe();  // 创建单个配方
    private int[] ParseIds(string input);     // 解析 ID 输入
}
```

## Data Models

### 修改标记结构

```csharp
// 用于批量修改工具的字段修改标记
public class ModifyFieldState
{
    public bool enabled;      // 是否启用修改
    public object value;      // 新值
    public string fieldName;  // 字段名
}
```

### 同步结果结构

```csharp
public struct SyncResult
{
    public int itemCount;      // 物品总数
    public int recipeCount;    // 配方总数
    public int newItemCount;   // 新增物品数
    public int newRecipeCount; // 新增配方数
    public bool success;       // 是否成功
    public string message;     // 结果消息
}
```

## Correctness Properties

*A property is a characteristic or behavior that should hold true across all valid executions of a system-essentially, a formal statement about what the system should do. Properties serve as the bridge between human-readable specifications and machine-verifiable correctness guarantees.*

### Property 1: SO 类型识别正确性
*For any* Selection.objects 集合，批量修改工具识别出的 SO 数量应等于集合中 ItemData 及其子类实例的数量
**Validates: Requirements 1.1**

### Property 2: 参数修改隔离性
*For any* SO 资产和修改标记集合，应用修改后，只有标记为 enabled=true 的字段值发生变化，其他字段保持原值
**Validates: Requirements 1.2**

### Property 3: 类型到字段映射一致性
*For any* ItemData 子类型（ToolData、WeaponData 等），返回的专属字段集合应与该类型定义的字段一致
**Validates: Requirements 1.5**

### Property 4: 数据库同步完整性
*For any* 批量创建的 SO 资产集合，同步后 ItemDatabase.allItems 应包含所有新创建的资产
**Validates: Requirements 2.1, 4.1**

### Property 5: ID 序列生成正确性
*For any* 起始 ID 和数量 n，连续 ID 模式生成的序列应为 [startID, startID+1, ..., startID+n-1]
**Validates: Requirements 3.7**

### Property 6: 配方输入解析正确性
*For any* 有效的配方输入字符串（ID、名称、产物），解析后的 RecipeData 字段值应与输入一一对应
**Validates: Requirements 3.4**

### Property 7: 配方数据库同步完整性
*For any* 批量创建的 RecipeData 资产集合，同步后 ItemDatabase.allRecipes 应包含所有新创建的配方
**Validates: Requirements 3.5**

## Error Handling

### 数据库不存在
- 检测 `MasterItemDatabase.asset` 是否存在
- 不存在时显示警告对话框，提示用户创建
- 提供快捷按钮跳转到创建菜单

### SO 类型不匹配
- 批量修改时检测选中对象是否为 ItemData 或其子类
- 过滤非 SO 对象，仅处理有效资产

### ID 冲突检测
- 创建前检查 ID 是否已存在于数据库
- 冲突时提示用户选择覆盖或跳过

### 文件写入失败
- 捕获 AssetDatabase 操作异常
- 显示详细错误信息和失败文件路径

## Testing Strategy

### 单元测试

使用 Unity Test Framework (NUnit) 进行单元测试：

1. **ID 解析测试**
   - 测试连续 ID 模式的序列生成
   - 测试单行/多行输入解析
   - 测试空输入和无效输入处理

2. **类型映射测试**
   - 测试各 SO 类型返回正确的字段集合
   - 测试未知类型的回退处理

3. **修改隔离测试**
   - 测试只有勾选字段被修改
   - 测试未勾选字段保持原值

### 属性测试

使用 FsCheck 或 NUnit 的 TestCaseSource 进行属性测试：

1. **Property 2: 参数修改隔离性**
   - 生成随机 SO 数据和随机修改标记
   - 验证修改后的字段变化符合预期

2. **Property 5: ID 序列生成正确性**
   - 生成随机起始 ID 和数量
   - 验证序列正确性

3. **Property 6: 配方输入解析正确性**
   - 生成随机有效输入
   - 验证解析结果与输入一致

### 集成测试

在 Unity Editor 中手动测试：

1. 创建测试 SO 资产
2. 执行批量修改
3. 验证数据库同步结果
4. 检查控制台日志输出

## File Structure

```
Assets/Editor/
├── Tool_BatchItemSOGenerator.cs      # 现有，需优化
├── Tool_BatchItemSOModifier.cs       # 新增：批量修改工具
├── Tool_BatchRecipeCreator.cs        # 新增：配方批量创建
└── DatabaseSyncHelper.cs             # 新增：数据库同步辅助

Assets/Scripts/Data/Database/
└── ItemDatabase.cs                   # 现有，不修改源码
```

## UI Design

### 批量修改工具 UI 布局

```
┌─────────────────────────────────────────┐
│  📝 批量修改物品 SO                      │
├─────────────────────────────────────────┤
│ 🖼️ 选中的 SO（自动跟随 Project 选择）    │
│ ┌─────────────────────────────────────┐ │
│ │ ✓ Tool_1_Axe_1 (ToolData)          │ │
│ │ ✓ Tool_2_Axe_2 (ToolData)          │ │
│ │ ✓ Tool_3_Axe_3 (ToolData)          │ │
│ └─────────────────────────────────────┘ │
├─────────────────────────────────────────┤
│ ⚙️ 通用属性（勾选才修改）                │
│ ☐ 价格    买 [___] 卖 [___]            │
│ ☐ 最大堆叠数 [___]                      │
│ ☐ 描述    [________________]           │
├─────────────────────────────────────────┤
│ 🔧 工具专属（检测到 ToolData）           │
│ ☐ 精力消耗 [___]                        │
│ ☐ 作用范围 [___]                        │
│ ☐ 动画帧数 [___]                        │
├─────────────────────────────────────────┤
│        [🚀 应用修改到 3 个 SO]           │
└─────────────────────────────────────────┘
```

### 配方批量创建工具 UI 布局

```
┌─────────────────────────────────────────┐
│  📜 批量创建配方 SO                      │
├─────────────────────────────────────────┤
│ 🔢 ID 设置                              │
│ ☑ 连续 ID 模式                          │
│ 起始 ID [8000]                          │
├─────────────────────────────────────────┤
│ 📝 配方信息（按行输入）                  │
│ 配方名称：                              │
│ ┌─────────────────────────────────────┐ │
│ │ 铜锭                                │ │
│ │ 铁锭                                │ │
│ │ 金锭                                │ │
│ └─────────────────────────────────────┘ │
│ 产物 ID：                               │
│ ┌─────────────────────────────────────┐ │
│ │ 3100                                │ │
│ │ 3101                                │ │
│ │ 3102                                │ │
│ └─────────────────────────────────────┘ │
├─────────────────────────────────────────┤
│ 🧪 共享材料（所有配方使用相同材料）      │
│ ┌─────────────────────────────────────┐ │
│ │ 材料 ID: [3000] 数量: [5]    [✖]   │ │
│ │ [+ 添加材料]                        │ │
│ └─────────────────────────────────────┘ │
├─────────────────────────────────────────┤
│ 🏭 制作设施                             │
│ [Furnace ▼]                             │
├─────────────────────────────────────────┤
│ 📁 输出设置                             │
│ 输出文件夹 [Assets/Data/Recipes] [选择] │
├─────────────────────────────────────────┤
│        [🚀 创建 3 个配方 SO]            │
└─────────────────────────────────────────┘
```

