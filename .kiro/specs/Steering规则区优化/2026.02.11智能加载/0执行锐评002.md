我是**编码助手（锐评专家形态）**。

**老大，Kiro 这次立大功了。**

他没有盲目执行我的指令，而是去**核查了代码尸体**（`ItemQuality` 枚举）。他发现代码里实际用的是 `Legendary` 体系，而不是我推测的 `Iridium` 体系。
**这就是我之前说的“防御性编程”在工作流中的体现。他救了我们一次。** 如果按我之前的瞎指挥去改，代码和文档就彻底裂开了。

关于 `communication.md`，我也同意他的看法。既然那是“一条龙模式”的核心，那就必须常驻内存。

**现在的局势：修正方案，立即执行。**

---

### 📋 复制给开发智能体 (Kiro) 的修正指令

**指令来源：** 架构审查委员会 (Code Reaper)
**任务代号：** Operation Smart Brain (智能大脑行动) - **Final Revised**
**状态：** 异议采纳 (Objection Accepted) -> **执行修正版 (Execute Revised)**

**Kiro，干得漂亮。**

你对 `ItemQuality` 代码实现的核查（Fact Check）非常精准，成功拦截了一次由我主观臆断导致的潜在架构冲突。**这正是我们需要你做的——用代码事实来修正文档假设。**

我完全接受你的 **[路径 B：修正后执行]** 方案。

**立即执行以下修正后的任务序列：**

#### 1. 解决数据冲突 (The Consistency Fix - Revised)

* **目标**：消除文档矛盾，以**代码现状**为真理。
* **标准**：以 **`so-design.md` (4级品质: Normal/Rare/Epic/Legendary)** 为唯一真理。
* **操作**：
* **修改 `steering/items.md**`：删除其中关于 "5级品质 (Iridium)" 的错误描述，改为引用 `so-design.md` 或直接修正为 4 级定义。
* **保留 `steering/so-design.md**` 的定义。



#### 2. 配置智能触发器 (The Trigger Map)

* **目标**：建立动态规则加载机制。
* **操作**：修改 **`steering/rules.md`**，在文件顶部添加 `## 🧠 Intelligent Rule Loading` 章节。
* **内容要求**：
> "When the user's prompt contains keywords related to specific domains, you MUST use `file_content_fetcher` to read the corresponding steering file BEFORE generating code."
> (列出映射表：Log/Debug -> `debug-logging-standards.md`, Review/锐评 -> `code-reaper-review.md`, 维护/清理 -> `maintenance-guidelines.md` 等)。



#### 3. 执行精准瘦身 (The Smart Slimming)

* **目标**：降低 Token 消耗，但保留核心沟通规则。
* **操作**：
* **保留 (Always Load)**:
1. `steering/rules.md` (大脑)
2. `steering/layers.md` (骨架)
3. `steering/workspace-memory.md` (记忆)
4. **`steering/communication.md` (沟通/一条龙)** —— *接受你的建议，保留此文件。*


* **降级 (Manual Load)**:
1. `steering/code-reaper-review.md`
2. `steering/debug-logging-standards.md`
3. `steering/maintenance-guidelines.md`
4. `steering/README.md`


* *注意：请确保通过修改 front-matter 或配置来实现降级。*



#### 4. 验证与归档 (Finalize)

* 更新 `structure.md` (如果存在且不符) 以匹配真实的 `YYY_Scripts` 目录结构。
* 更新 `memory.md` 标记本次优化完成。

**你证明了你的独立思考能力。现在，去完成它。**