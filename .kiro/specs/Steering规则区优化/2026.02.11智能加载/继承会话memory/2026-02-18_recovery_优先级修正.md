# 继承快照 - 2026-02-18 000-context-recovery.md 优先级修正（未完成）

## 1. 用户 prompt 完整摘抄

当然也不是百分百可以信任的权威，你当然需要有正当的存疑，如果其中有memory和快照关键内容不一致，你就直接停止向我询问，但是如果你发现是memory没有正确更新停留在上一个对话，然后快照内包含了上一个对话的内容，并且快照的内容是其下一个会话那你需要向我询问是否需要更新memory，这些都是很灵活的情况，如果会话继承内容和前两个文件非常不一致或者是毫不相干你也要停下来询问我，总之你自行完善情况链

## 2. AI 进度摘要

**已完成**：
- 优先级 1 措辞修改：从"绝对权威·100%信任"改为"最高权重·需交叉验证"（strReplace 已执行成功）

**未完成**：
- 异常情况判断链章节尚未添加（用户要求的核心内容）
- 用户要求的具体场景：
  1. memory 和快照关键内容不一致 → 停止，向用户询问
  2. memory 停留在上一个对话 + 快照包含上一个对话内容且是下一个会话 → 询问用户是否需要更新 memory
  3. 系统继承摘要与快照/memory 完全不一致或毫不相干 → 停止，向用户询问
  4. 其他灵活情况需自行完善

## 3. 修改文件列表

- `000-context-recovery.md` - 优先级 1 措辞已修改（strReplace），异常判断链未添加

## 4. 本轮关键决策与用户偏好

- 用户明确：快照不是"盲信"，而是"最高权重但仍需交叉验证"
- 用户要求：遇到不一致时必须停下来询问，不能自作主张
- 用户风格：要求灵活处理，不是死板规则，AI 需要自行完善情况链

## 5. 关联文件与待同步状态

- `000-context-recovery.md` - 修改进行中（优先级 1 已改，异常判断链待添加）
- 主 memory 已有上一轮记录（重写信息源优先级强化），本轮修改尚未记录

## 6. 遗留问题完整上下文

- 异常情况判断链是本轮核心任务，被压缩中断
- 上一轮已完成的重写（禁止幻想铁律等）已生效
- 本轮是在上一轮基础上的进一步完善：从"绝对权威"降级为"最高权重+交叉验证+异常停止"
