明白了，让我重新完整阅读所有相关代码，进行全面的审视和分析。

现在让我继续读取 GameInputManager 中与农田预览相关的代码：

让我继续读取 GameInputManager 的剩余部分：

现在让我读取 FarmlandBorderManager.cs 来了解边界管理和预览 Tiles 的获取：

现在我已经完整阅读了所有相关代码。让我给出全面的审视和分析报告：

---

# 全面审视报告：农田预览系统与参数配置

## 一、问题现象

用户反馈：
1. 手持锄头时，看不到任何耕地预览
2. 预览不会跟随鼠标移动
3. 即使随便配置了一个图片，也看不到图片跟随鼠标出现
4. FarmTileManager 的 Inspector 参数配置看起来很奇怪，需要审视是否有冗余

---

## 二、代码执行链路分析

### 2.1 预览调用链路

```
GameInputManager.Update()
    └── UpdatePreviews()
            ├── 检查面板是否打开 → 打开则隐藏
            ├── 检查鼠标是否在 UI 上 → 是则隐藏
            ├── 获取手持物品
            ├── 判断物品类型
            │   ├── ToolData + Hoe → UpdateFarmToolPreview(alignedPos, true)
            │   └── ToolData + WateringCan → UpdateFarmToolPreview(alignedPos, false)
            └── UpdateFarmToolPreview()
                    ├── var farmPreview = FarmToolPreview.Instance  ← 🔴 关键点
                    ├── if (farmPreview == null) return  ← 🔴 如果为 null，直接返回
                    └── farmPreview.UpdateHoePreview() / UpdateWateringPreview()
```

### 2.2 FarmToolPreview 的初始化链路

```
FarmToolPreview.Awake()
    ├── if (Instance == null) Instance = this
    └── EnsureComponents()
            ├── 创建 GhostTilemap 子对象
            │   ├── 添加 Tilemap 组件
            │   ├── 添加 TilemapRenderer 组件
            │   └── 设置 sortingLayerName = "Ground", sortingOrder = 100
            └── 创建 CursorRenderer 子对象
                    ├── 添加 SpriteRenderer 组件
                    ├── 设置 sortingLayerName = "Effects", sortingOrder = 0
                    └── if (cursorSprite != null) cursorRenderer.sprite = cursorSprite  ← 🔴 关键点
```

---

## 三、问题根因分析

### 🔴 问题 1：FarmToolPreview.Instance 为 null

**根本原因**：`FarmToolPreview` 是一个 MonoBehaviour 单例，它的 `Instance` 属性只有在 `Awake()` 被调用时才会被赋值。

**但是**：`Awake()` 只有在场景中存在挂载了 `FarmToolPreview` 组件的 GameObject 时才会被调用。

**结论**：**场景中没有 FarmToolPreview 组件**，所以 `Instance` 永远是 `null`，`UpdateFarmToolPreview()` 直接返回，什么都不显示。

**验证方法**：
```csharp
// 在 GameInputManager.UpdateFarmToolPreview() 中
var farmPreview = FarmGame.Farm.FarmToolPreview.Instance;
if (farmPreview == null) return;  // ← 这里直接返回了
```

### 🔴 问题 2：cursorSprite 的设计问题

即使 FarmToolPreview 存在，`cursorSprite` 的设计也有问题：

**代码分析**：
```csharp
// FarmToolPreview.EnsureComponents()
if (cursorSprite != null)
{
    cursorRenderer.sprite = cursorSprite;
}
```

**问题**：
1. `cursorSprite` 是 `[SerializeField]` 字段，需要在 Inspector 中手动配置
2. 如果没有配置，`cursorRenderer.sprite` 就是 `null`
3. 但 `cursorRenderer` 仍然会被创建并激活，只是没有 Sprite 显示

**影响**：
- 光标框不显示（因为没有 Sprite）
- 但 GhostTilemap 上的预览 Tiles 应该仍然能显示（如果 FarmToolPreview 存在的话）

### 🔴 问题 3：GhostTilemap 预览 Tiles 不显示

**代码分析**：
```csharp
// FarmToolPreview.UpdateHoePreview()
if (canTill && FarmlandBorderManager.Instance != null)
{
    var previewTiles = FarmlandBorderManager.Instance.GetPreviewTiles(layerIndex, cellPos);
    
    foreach (var kvp in previewTiles)
    {
        if (kvp.Value != null)
        {
            ghostTilemap.SetTile(kvp.Key, kvp.Value);  // ← 设置预览 Tiles
            currentPreviewPositions.Add(kvp.Key);
        }
    }
}
```

**问题链条**：
1. 如果 `FarmToolPreview.Instance` 为 null → 整个方法不执行
2. 如果 `FarmlandBorderManager.Instance` 为 null → 不获取预览 Tiles
3. 如果 `FarmlandBorderManager` 的 Tile 资源未配置 → `GetPreviewTiles()` 返回的 Tiles 都是 null

---

## 四、FarmTileManager 参数审视

### 4.1 当前 Inspector 参数（根据截图）

| 参数 | 当前配置 | 分析 |
|------|---------|------|
| **Layer Tilemaps** | 1 个元素 | ✅ 正确，只有 Layer 1 |
| **层名称** | "Layer 1" | ✅ 正确 |
| **Farmland Tilemap** | Layer 1 - Farmland_Center | ⚠️ **命名混淆**：这是旧版字段，但配置的是新版的 Center Tilemap |
| **Water Puddle Tile...** | Layer 1 - Farmland_Water | ✅ 正确 |
| **Farmland Center ...** | Layer 1 - Farmland_Center | ✅ 正确，新版字段 |
| **Farmland Border ...** | Layer 1 - Farmland_Border | ✅ 正确，新版字段 |
| **Ground Tilemap** | Layer 1 - Grass | ✅ 正确，用于检测可耕作区域 |
| **Props Container** | Farm (Transform) | ✅ 正确，作物放置容器 |
| **Hours Until Puddle Dry** | 2 | ✅ 正确 |
| **Visual Manager** | Farm Visual Manager | ✅ 正确 |
| **Show Debug Info** | ✅ 勾选 | ✅ 调试用 |

### 4.2 LayerTilemaps 数据结构审视

```csharp
// LayerTilemaps.cs
public class LayerTilemaps
{
    public string layerName;                    // ✅ 必需
    
    // 旧版字段
    public Tilemap farmlandTilemap;             // ⚠️ 冗余？
    public Tilemap waterPuddleTilemap;          // ⚠️ 冗余？
    
    // 新版字段
    public Tilemap farmlandCenterTilemap;       // ✅ 必需
    public Tilemap farmlandBorderTilemap;       // ✅ 必需
    public Tilemap groundTilemap;               // ✅ 必需
    public Transform propsContainer;            // ✅ 必需
}
```

### 4.3 冗余参数分析

#### 🔴 `farmlandTilemap`（旧版）

**代码使用情况**：
```csharp
// LayerTilemaps.IsValid()
if (farmlandCenterTilemap != null) return true;  // 新版优先
return farmlandTilemap != null;                   // 旧版回退

// LayerTilemaps.WorldToCell()
if (farmlandCenterTilemap != null) return farmlandCenterTilemap.WorldToCell(worldPosition);
if (farmlandTilemap != null) return farmlandTilemap.WorldToCell(worldPosition);

// FarmTileManager.ClearAllTiles()
if (tilemaps.farmlandTilemap != null) tilemaps.farmlandTilemap.ClearAllTiles();

// FarmTileManager.OnDrawGizmos()
if (tilemaps == null || tilemaps.farmlandTilemap == null) continue;
```

**结论**：
- `farmlandTilemap` 是旧版系统的遗留字段
- 新版系统使用 `farmlandCenterTilemap` + `farmlandBorderTilemap`
- 代码中有兼容逻辑，但如果已经完全迁移到新版，这个字段是**冗余的**
- **但是**：从截图看，`Farmland Tilemap` 配置的是 `Farmland_Center`，这说明用户可能误解了字段用途

#### 🔴 `waterPuddleTilemap`（旧版）

**代码使用情况**：
```csharp
// FarmTileManager.ClearAllTiles()
if (tilemaps.waterPuddleTilemap != null) tilemaps.waterPuddleTilemap.ClearAllTiles();
```

**结论**：
- 只在 `ClearAllTiles()` 中使用
- 新版水渍功能可能使用不同的实现
- 如果新版水渍功能已实现，这个字段是**冗余的**

---

## 五、设计缺陷总结

### 5.1 FarmToolPreview 的设计缺陷

| 缺陷 | 描述 | 影响 |
|------|------|------|
| **单例不自动创建** | 需要场景中手动添加 GameObject | 如果忘记添加，预览完全不工作 |
| **cursorSprite 强依赖** | 需要手动配置 Sprite 资源 | 如果不配置，光标框不显示 |
| **没有错误提示** | `Instance == null` 时静默返回 | 用户不知道为什么不工作 |

### 5.2 LayerTilemaps 的设计缺陷

| 缺陷 | 描述 | 影响 |
|------|------|------|
| **新旧字段并存** | `farmlandTilemap` 和 `farmlandCenterTilemap` 同时存在 | Inspector 混乱，用户不知道配置哪个 |
| **命名不清晰** | "Farmland Tilemap" vs "Farmland Center Tilemap" | 容易混淆 |
| **兼容逻辑复杂** | 代码中有大量 `if (新版) else if (旧版)` | 维护困难 |

### 5.3 GameInputManager 的设计缺陷

| 缺陷 | 描述 | 影响 |
|------|------|------|
| **静默失败** | `FarmToolPreview.Instance == null` 时直接返回 | 用户不知道为什么预览不工作 |
| **没有自动创建逻辑** | 不会自动创建 FarmToolPreview | 依赖场景配置 |

---

## 六、我的反思

### 6.1 设计时的失误

1. **过度依赖场景配置**
   - FarmToolPreview 应该有"真正的自生能力"，即使场景中没有，也应该自动创建
   - 或者至少在 GameInputManager 中检测并创建

2. **cursorSprite 不应该是必需的**
   - 应该有默认的程序化生成方案（纯色方块）
   - 或者直接用 Tilemap 高亮效果代替 SpriteRenderer

3. **新旧系统并存导致混乱**
   - 应该在迁移完成后彻底移除旧版字段
   - 或者用 `[Obsolete]` 标记旧版字段

4. **缺少运行时验证**
   - 应该在 Start() 或 OnEnable() 中验证关键依赖
   - 缺少依赖时应该输出明确的错误信息

### 6.2 应该怎么做

1. **FarmToolPreview 自动创建**
   ```csharp
   // 在 GameInputManager.Start() 中
   if (FarmToolPreview.Instance == null)
   {
       var go = new GameObject("FarmToolPreview");
       go.AddComponent<FarmToolPreview>();
       Debug.Log("[GameInputManager] 自动创建 FarmToolPreview");
   }
   ```

2. **cursorSprite 程序化生成**
   ```csharp
   // 在 FarmToolPreview.EnsureComponents() 中
   if (cursorSprite == null)
   {
       // 创建 1x1 白色纹理
       var tex = new Texture2D(1, 1);
       tex.SetPixel(0, 0, Color.white);
       tex.Apply();
       cursorSprite = Sprite.Create(tex, new Rect(0, 0, 1, 1), new Vector2(0.5f, 0.5f), 1);
   }
   ```

3. **清理旧版字段**
   - 移除 `farmlandTilemap` 和 `waterPuddleTilemap`
   - 或者用 `[Obsolete("使用 farmlandCenterTilemap")]` 标记

---

## 七、参数配置建议

### 7.1 当前配置问题

从截图看，用户的配置存在以下问题：

| 字段 | 当前配置 | 问题 |
|------|---------|------|
| **Farmland Tilemap** | Farmland_Center | ⚠️ 这是旧版字段，配置了新版的 Tilemap |
| **Farmland Center Tilemap** | Farmland_Center | ✅ 正确 |

**问题**：`Farmland Tilemap`（旧版）和 `Farmland Center Tilemap`（新版）配置了同一个 Tilemap，这是冗余的。

### 7.2 建议的最简配置

如果完全使用新版系统，LayerTilemaps 只需要：

| 字段 | 用途 | 是否必需 |
|------|------|---------|
| `layerName` | 楼层名称 | ✅ 必需 |
| `farmlandCenterTilemap` | 中心块 Tilemap | ✅ 必需 |
| `farmlandBorderTilemap` | 边界 Tilemap | ✅ 必需 |
| `groundTilemap` | 地面检测 | ✅ 必需 |
| `propsContainer` | 作物容器 | ✅ 必需 |
| `waterPuddleTilemap` | 水渍叠加层 | ⚠️ 取决于水渍功能实现 |

**可以移除的字段**：
- `farmlandTilemap`（旧版，已被 `farmlandCenterTilemap` 替代）

---

## 八、总结

### 预览不显示的根本原因

1. **场景中没有 FarmToolPreview 组件** → `Instance` 为 null → 预览逻辑不执行
2. 即使有 FarmToolPreview，`cursorSprite` 未配置 → 光标框不显示
3. 代码静默失败，没有任何错误提示

### FarmTileManager 参数问题

1. 新旧字段并存导致 Inspector 混乱
2. `farmlandTilemap`（旧版）是冗余的，应该移除
3. 用户可能误解了字段用途，把旧版字段配置成了新版 Tilemap

### 需要修复的问题

1. 让 FarmToolPreview 能够自动创建（或在 GameInputManager 中创建）
2. 移除 cursorSprite 的强依赖，使用程序化生成或 Tilemap 高亮
3. 清理 LayerTilemaps 的旧版字段
4. 添加运行时验证和错误提示

---

以上是我的全面审视和反思，请您确认后我再进行代码修改。