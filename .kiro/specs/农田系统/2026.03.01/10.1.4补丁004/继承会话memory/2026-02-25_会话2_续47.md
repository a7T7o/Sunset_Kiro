# 继承会话快照 — 会话2续47

## 1. 用户 prompt 完整摘抄

> 先修复吧孩子，先修复最重要确实

用户选择了审视报告中的选项 A：先修 Bug 1/Bug 2（5行代码），再执行模块R（种子剥离到放置系统）。

## 2. AI 进度摘要

### 已完成
1. ✅ 用户决策确认：选项 A（先修 Bug 1/Bug 2）
2. ✅ 重新读取 ProcessNextAction 代码确认 Bug 位置

### 未完成
1. ❌ Bug 1 修复未执行：ProcessNextAction 中种子跳过时未清理 FarmToolPreview
2. ❌ Bug 2 修复未执行：ProcessNextAction 队列为空时未调用 ClearAllQueuePreviews
3. ❌ 编译验证未执行
4. ❌ memory 更新未完成（主 memory 续47未同步）

## 3. 修改文件列表

| 文件 | 操作 | 说明 |
|------|------|------|
| `memory.md`（子） | 追加 | 续47记录 |

## 4. 本轮关键决策与用户偏好

- 用户选择选项 A：先修 Bug 1/Bug 2，再执行模块R
- 用户态度轻松直接："先修复吧孩子"
- 确认种子走 FIFO 本质上是错的，但先解决眼前问题

## 5. 关联文件与待同步状态

| 文件 | 路径 | 状态 |
|------|------|------|
| 005锐评003审视报告 | `005锐评003审视报告.md` | ✅ 已创建（续46） |
| GameInputManager.cs | 核心代码 | ✅ 已读取 ProcessNextAction |
| FarmToolPreview.cs | 核心代码 | ❌ 未读取（修复时需要确认清理方法签名） |
| 子 memory | `memory.md` | ✅ 续47已追加 |
| 主 memory | `.kiro/specs/农田系统/memory.md` | ❌ 续47未同步 |

## 6. 遗留问题完整上下文

### Bug 1（🔴严重）：ProcessNextAction 跳过种子时队列预览残留

**位置**：ProcessNextAction 第2377-2385行，PlantSeed 二次验证分支

```csharp
case FarmActionType.PlantSeed:
    if (!HasSeedRemaining())
    {
        _queuedPositions.Remove((request.layerIndex, request.cellPos));
        ProcessNextAction(); // 跳过，继续下一个
        return;
    }
    break;
```

**问题**：跳过时只移除了 `_queuedPositions`，但没有清理 FarmToolPreview 的：
- `queuePreviewPositions`（b 层拦截数据源）
- 种子队列预览 SpriteRenderer（`activeSeedQueuePreviews`）

**修复方向**：在 `_queuedPositions.Remove` 之后增加 `FarmToolPreview.Instance?.RemoveQueuePreview(request.cellPos)` 或等效的种子预览清理调用

### Bug 2（🟡中等）：队列清空时未清理 FarmToolPreview

**位置**：ProcessNextAction 第2362-2369行，队列为空分支

```csharp
if (_farmActionQueue.Count == 0)
{
    _isProcessingQueue = false;
    _isExecutingFarming = false;
    _queuedPositions.Clear();
    _farmNavState = FarmNavState.Preview;
    return;
}
```

**问题**：`_queuedPositions.Clear()` 清空了 GameInputManager 的位置记录，但 FarmToolPreview 的 `queuePreviewPositions` 和队列预览 tile/SpriteRenderer 没有被清理

**修复方向**：在 `_queuedPositions.Clear()` 之后增加 `FarmToolPreview.Instance?.ClearAllQueuePreviews()` 调用

### 后续计划

Bug 1/Bug 2 修复后验收，然后执行模块R（种子从 FIFO 剥离到放置系统）。
