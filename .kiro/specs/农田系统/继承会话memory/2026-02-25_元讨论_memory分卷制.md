# 继承快照 - 元讨论：Memory 分卷制改造

> 对话时间特征：2026-02-25
> 工作区：.kiro/specs/农田系统/（主工作区层面元讨论）
> 创建时间：2026-02-25

## 用户 Prompt 完整摘抄

### Prompt 1（方案确认+参数调整）
我也认为是方案c，但是工作区其实就是不可避免的会增多，需要做到的只是限制单次，但对于总数量的限制应该是出于超过一定量就新建阶段，对于阶段命名可能最轻松的就是默认主memory是memory_0后续自增，然后其实子memory也会很长，所以子memory也默认memory_0，子memory如果超过一定行数也自增，你认为可以吗？当然子和主memory的总数限制可能需要定制，都是不一样的，你看分寸应该如何把握，因为读写最多的其实是子memory，然后每次对话完毕都要更新主memory，这是hook要做的，你分析一下呢？我认为主memory一卷承受25到30个即可，一卷300行，然后子memory一卷400行，承受对话数量根据对话量来改变，这个不做限制，然后当前的这个三千多行的主memory和一千多行的子memory肯定要进行优化，我建议当前的内容归档memory_0，然后后续的内容直接从memory_1开始，你认为可以吗？当然还需要修改规则，但是规则需要确保只是超限后的下一个对话进入新卷，不是截断严格的300行，而是下一个快超过或者已经超过才进入新卷，我们肯定是为了不影响准确度和基础功能的前提来完成，当然前情提要和最近的上一个对话的内容如果很贴近也是完全可以继承的，我认为没毛病，你灵活处理，全面分析即可

### Prompt 2（workspace-memory质疑）
我几乎认可你的方案，只有对于你说会智能加载workspace-memory这个规则我不认可，因为没有任何的内容是需要调用这个的，我至今没遇到过你加载这个规则，我不会说一个指令来让你单纯修改工作区规则，让你生成两件套的时候你也不会参照这个，更新检查的时候也是按照hook来，可能这个workspace-memory就是需要常驻？还是应该让rule记录这个内容？我认为这是当前的问题所在，请你先回应我

### Prompt 3（最终确认+开始执行）
我认为这是很合理的处理方式，但是rule里面对于workspace-memory的内容的权重可能需要提升，不只是新增对于当前归档和memory规则的内容，而是全面重新审视workspace-memory的占比和描述情况，并让workspace-memory彻底融入rule，当然也不是写个五十行进去，我的意思是把重要的关键的我们之前遗漏的写进入，确保我们的workspace-memory是间接有效的就行，你认为呢？其他的都认可了，比如Hook prompt 里加入分卷检查逻辑、context-recovery 里加入分卷感知、以及上个对话你说的所有的方案我都认可，请你直接开始吧，哦对了我们当前所有内容都没有记入任何的memory，我们的讨论消失了，请你再新增一个任务，让memory更新检查的规则不在限制于是否有工作内容，方案讨论本身就很有意义，所以我希望你的memory只检查工作的时候有没有更新，如果没有更新则进行更新，也就是所有的内容所有的对话都计入，这是很关键的，请你注意，然后顺序就是先更新hook，然后再更新workspace-memory，然后就是完成剩余的内容了，请你开始吧

## AI 进度摘要

### 已完成
1. ✅ memory-update-check.kiro.hook 更新为 v9.0（分卷检查 + 全对话记录 + 主memory摘要限制）
2. ✅ rules.md 中"工作区记忆"部分重写（融入 workspace-memory 关键规则：分卷制、更新规则、禁令）
3. ✅ 000-context-recovery.md 加入分卷感知规则（优先级2下新增分卷处理说明）
4. ✅ 农田系统主 memory 归档（3267行 memory.md → memory_0.md，新建精简 memory.md 约80行）
5. ✅ 农田系统主 memory.md 追加元讨论会话记录（记录员补记）

### 未完成
1. ❌ 10.1.4补丁004 子 memory 归档（1774行 → memory_0.md + 新建精简 memory.md）
2. ❌ 其他需要归档的子 memory（如有）

## 修改文件列表

| 文件 | 操作 | 说明 |
|------|------|------|
| `.kiro/hooks/memory-update-check.kiro.hook` | 修改 | v8.0→v9.0，加入分卷检查+全对话记录+主memory摘要限制 |
| `.kiro/steering/rules.md` | 修改 | "工作区记忆"部分重写，融入workspace-memory关键规则 |
| `.kiro/steering/000-context-recovery.md` | 修改 | 优先级2下新增分卷感知规则 |
| `.kiro/specs/农田系统/memory_0.md` | 新增 | 主memory归档（原3267行memory.md） |
| `.kiro/specs/农田系统/memory.md` | 重建 | 新卷，精简前情提要+子工作区索引 |

## 本轮关键决策与用户偏好

1. **分卷参数确定**：主memory 200行/卷，子memory 300行/卷
2. **命名方案**：活跃卷始终是 memory.md，归档为 memory_0/1/2...
3. **workspace-memory定位**：参考文档而非运行时依赖，关键规则融入 rules.md 常驻
4. **全对话记录**：移除"无实质工作则跳过"逻辑，所有对话都必须记录
5. **主memory单条限制**：3-5行（一句话主题+结论+关键文件）

## 关联文件与待同步状态

| 文件 | 状态 |
|------|------|
| `.kiro/steering/workspace-memory.md` | 未修改（保留为参考文档） |
| `.kiro/specs/农田系统/10.1.4补丁004/memory.md` | 待归档（1774行） |

## 遗留问题完整上下文

1. **10.1.4补丁004 子 memory 归档**：1774行需要归档为 memory_0.md + 新建精简 memory.md。AI 在执行中被压缩中断，需要继承后继续完成。
2. **其他子工作区 memory 检查**：可能还有其他超限的子 memory 需要归档，但用户只明确提到了主 memory 和 10.1.4补丁004。
