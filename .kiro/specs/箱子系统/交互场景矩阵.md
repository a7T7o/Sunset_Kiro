# 箱子系统 - 交互场景矩阵

## 概述

本文档厘清箱子系统在不同场景下的所有交互行为，对比需求与当前实现状态。

**核心原则**：
1. 箱子 UI 与背包面板互斥显示
2. Box UI 在 PackagePanel 内部，不是 Canvas 根目录
3. Down 区域显示玩家完整背包（0-35），Hotbar 是背包第一行的映射
4. **面板打开时禁用世界输入**（已实现，用户要求详细记录）

---

## 面板状态机（锐评后新增）

### 状态定义

| 状态 | 说明 | panelRoot | Main/Top | Box UI |
|------|------|-----------|----------|--------|
| NoPanel | 无 UI 打开 | inactive | - | - |
| BackpackOpen | 背包面板打开 | active | visible | - |
| BoxOpen | 箱子 UI 打开 | active | hidden | visible |

### 状态转换矩阵

| 当前状态 | Tab | ESC | 点击箱子 | 点击世界 |
|---------|-----|-----|---------|---------|
| **NoPanel** | → BackpackOpen | 打开设置 | → BoxOpen | 正常交互 |
| **BackpackOpen** | → NoPanel | → NoPanel | → BoxOpen | ❌ 禁用 |
| **BoxOpen** | → BackpackOpen | → NoPanel | 切换箱子 | ❌ 禁用 |

### 关键转换路径

1. **NoPanel → BoxOpen**：
   - 触发：点击箱子
   - 行为：`OpenBoxUI()` → `EnsurePanelOpenForBox()` → `HideMainAndTop()` → 实例化 Box UI

2. **BoxOpen → BackpackOpen**：
   - 触发：按 Tab/B/M/L/O
   - 行为：`OpenOrToggle(idx)` → `CloseBoxPanelIfOpen()` → 销毁 Box UI → `ShowMainAndTop()` → `SetVisiblePage(idx)`
   - **实现要点**：
     - `OpenOrToggle()` 开头必须调用 `CloseBoxPanelIfOpen()`
     - `CloseBoxPanelIfOpen()` 负责销毁 Box UI 并恢复 Main/Top
     - 不允许 Box 和背包 Main/Top 同时可见

3. **BoxOpen → NoPanel**：
   - 触发：按 ESC
   - 行为：`CloseBoxUI(openBackpackAfter: false)` → 销毁 Box UI → `ClosePanel()`

4. **BackpackOpen → BoxOpen**：
   - 触发：点击箱子
   - 行为：记录 `_wasBackpackOpenBeforeBox = true` → `HideMainAndTop()` → 实例化 Box UI

---

## 场景定义

### 场景 1：世界（无 UI 打开）
- 玩家在世界中自由移动
- Toolbar 可见
- 可以与箱子交互

### 场景 2：背包面板打开
- PackagePanel 激活
- 世界被遮挡
- 箱子 UI 不可见

### 场景 3：箱子面板打开
- BoxPanelUI 激活
- PackagePanel 的 Main/Top 被隐藏
- PackagePanel 根（panelRoot）保持 active
- 世界被遮挡

### 场景 4：拿起物品状态（箱子内）
- 玩家正在拖拽物品
- 可以在箱子和背包之间转移

---

## 输入矩阵

### 场景 1：世界（无 UI 打开）

| 输入 | 期望行为 | 当前实现 | 状态 |
|------|----------|----------|------|
| **右键（箱子）** | 自动导航 → 打开箱子 UI | GameInputManager.HandleInteractable | ✅ |
| **右键（远距离箱子）** | 自动导航到箱子 | AutoNavigation | ✅ |
| **左键（箱子）** | 使用工具攻击箱子 | ChestController.OnHit | ✅ |
| **Tab/B/M/L/O** | 打开背包对应页面 | HandlePanelHotkeys | ✅ |
| **ESC** | 打开设置页面 | HandlePanelHotkeys | ✅ |

---

### 场景 2：背包面板打开

| 输入 | 期望行为 | 当前实现 | 状态 |
|------|----------|----------|------|
| **右键（世界）** | ❌ 禁用 | IsAnyPanelOpen 检查 | ✅ |
| **ESC** | 关闭背包 | HandlePanelHotkeys | ✅ |
| **Tab** | 关闭背包（如果在 Props 页） | OpenOrToggle | ✅ |

---

### 场景 3：箱子面板打开

| 输入 | 期望行为 | 当前实现 | 状态 |
|------|----------|----------|------|
| **WASD** | ❌ 禁用 | IsAnyPanelOpen 检查 | ✅ |
| **左键（箱子槽位 Up）** | 拾取/放置物品 | InventorySlotUI | ⚠️ 待验证 |
| **左键（背包槽位 Down）** | 拾取/放置物品 | InventorySlotUI | ❌ **槽位未配置** |
| **右键（世界）** | ❌ 禁用 | IsAnyPanelOpen 检查 | ✅ |
| **ESC** | 关闭箱子 UI | HandlePanelHotkeys | ✅ |
| **Tab/B/M/L/O** | 关闭箱子 → 打开背包 | CloseBoxPanelIfOpen | ✅ |
| **滚轮** | ❌ 禁用 | pointerOverUI 检查 | ✅ |
| **1-5** | ❌ 禁用 | 已修复 | ✅ |

---

### 场景 4：拿起物品状态（箱子内）

| 输入 | 期望行为 | 当前实现 | 状态 |
|------|----------|----------|------|
| **左键（箱子空槽位）** | 放置物品 | InventoryInteractionManager | ⚠️ 待验证 |
| **左键（背包空槽位）** | 放置物品到背包 | InventoryInteractionManager | ❌ **槽位未配置** |
| **左键（面板外）** | 丢弃物品 | DropItem | ⚠️ 待验证 |
| **ESC** | 取消拿取，返回原位 | ReturnToSource | ⚠️ 待验证 |

---

## 面板互斥逻辑

### 打开箱子时

```
ChestController.OnInteract()
└── PackagePanelTabsUI.OpenBoxUI(boxUiPrefab)
    ├── EnsurePanelOpenForBox()          // 确保 panelRoot active
    │   └── OnPanelJustOpened()          // 初始化背包服务
    ├── HideMainAndTop()                 // 隐藏背包区域
    └── 实例化 Box UI 到 boxUIRoot
        └── BoxPanelUI.Open(chest)
            └── RefreshUI()
                ├── RefreshChestSlots()      // ✅ 正常
                └── RefreshInventorySlots()  // ❌ 收集到 0 个槽位
```

### 打开背包时

```
PackagePanelTabsUI.OpenPanel()
├── CloseBoxPanelIfOpen()                // 关闭箱子 UI
├── panelRoot.SetActive(true)
└── OnPanelJustOpened()
```

### 验收点

| 检查项 | 期望 | 当前状态 |
|--------|------|----------|
| 右键箱子后 panelRoot.activeSelf | true | ✅ |
| 右键箱子后 topParent.activeSelf | false | ✅ |
| 右键箱子后 pagesParent.activeSelf | false | ✅ |
| Box UI 出现在 boxUIRoot 下 | 可见 | ✅ |
| Up 区域显示箱子内容 | 正确 | ✅ |
| Down 区域显示背包内容 | 正确 | ❌ **空白** |

---

## 🔴 当前问题汇总（2026-01-16 更新）

### 问题 1：Up 区域收集到 0 个槽位

**现象**：
```
[BoxPanelUI] 收集到 0 个箱子槽位  ← Up 区域问题！
```

**根源**：Box UI 预制体的 **Up 区域**子物体缺少 `InventorySlotUI` 组件。

**用户疑虑**：担心添加 `InventorySlotUI` 后会"吞食"背包内容。

**修复方**：用户配置预制体（待确认是否需要）

### 问题 2：Down 区域只显示第二行

**现象**：Down 区域只显示索引 12-35 的物品，第一行（0-11）不显示。

**根源**：代码 `startIndex = 12` 跳过了第一行。

**用户澄清**：
> Hotbar 是背包第一行的映射，完全共用，这是 UI 最初设计就规定的。
> Down 应显示完整背包（0-35），当前代码 `startIndex=12` 是错误的。

**修复方**：代码修改（`startIndex` 从 12 改为 0）

### 问题 3：关闭箱子后 Tab 无法打开背包

**现象**：打开箱子 → 按 Tab 关闭 → 再按 Tab 无法打开背包。

**根源**：`CloseBoxUI()` 后 Main/Top 未正确恢复激活状态。

**修复方**：代码修改（确保 `ShowMainAndTop()` 被正确调用）

### 问题 4：箱子交互只能点击 Collider

**现象**：只有点击箱子底部（Collider 区域）才能触发交互，点击上半部分无反应。

**根源**：`GameInputManager.HandleInteractable()` 使用 `Physics2D.OverlapPointAll` 检测，只能检测到 Collider。

**用户询问**：是否需要给箱子父物体添加触发器，还是修改脚本？

**修复方**：代码修改（使用 `SpriteRenderer.bounds` 替代 Collider 检测）

### 预制体结构对比

**期望结构**：
```
Box_24 (UI Prefab Root)
├── Up              (GridLayoutGroup)
│    ├── Up_00      (Toggle + InventorySlotUI + Image)  ❌ 缺少 InventorySlotUI
│    ├── Up_01      (Toggle + InventorySlotUI + Image)
│    └── ...
└── Down            (GridLayoutGroup)
     ├── Down_00    (Toggle + InventorySlotUI + Image)  ✅ 已配置
     ├── Down_01    (Toggle + InventorySlotUI + Image)
     └── ... (共 36 个槽位)
```

**当前结构**：
```
Box_24 (UI Prefab Root)
├── Up              (GridLayoutGroup)
│    ├── Up_00      (Toggle + Image)  ❌ 缺少 InventorySlotUI！
│    └── ...
└── Down            (GridLayoutGroup)
     ├── Down_00    (Toggle + InventorySlotUI + Image)  ✅
     └── ...
```

---

## 修复方案

### 方案 A：手动配置预制体（推荐）

在 Unity 编辑器中，为每个 Box UI 预制体的 Down 区域子物体添加 `InventorySlotUI` 组件：

1. 打开 `Assets/222_Prefabs/UI/Box_12.prefab`
2. 展开 Down 节点
3. 选中所有 Down_XX 子物体
4. 添加 `InventorySlotUI` 组件
5. 对 Box_24, Box_36, Box_48 重复以上步骤

### 方案 B：代码自动添加组件（不推荐）

在 `CollectSlots()` 中自动添加缺失的组件。
**不推荐**：违反"预制体绑定，不动态生成"原则。

---

## 需求实现状态总览

### 需求 1：面板互斥逻辑

| 验收标准 | 状态 | 说明 |
|---------|------|------|
| Box 打开时隐藏 PackagePanel Main/Top | ✅ | HideMainAndTop() |
| PackagePanel 打开时关闭 Box | ✅ | CloseBoxPanelIfOpen() |
| ESC 关闭 Box | ✅ | HandlePanelHotkeys |
| 背包键关闭 Box 并打开 PackagePanel | ✅ | HandlePanelHotkeys |

### 需求 2：ChestInventory 事件系统

| 验收标准 | 状态 | 说明 |
|---------|------|------|
| 槽位变化触发 OnSlotChanged | ✅ | ChestInventory 已实现 |
| 整体变化触发 OnInventoryChanged | ✅ | ChestInventory 已实现 |
| BoxPanelUI 订阅事件 | ✅ | SubscribeToChest() |
| BoxPanelUI 取消订阅 | ✅ | UnsubscribeFromChest() |

### 需求 3：不同容量 UI 适配

| 验收标准 | 状态 | 说明 |
|---------|------|------|
| 12 格箱子显示 1×12 | ✅ | Box_12 预制体 |
| 24 格箱子显示 2×12 | ✅ | Box_24 预制体 |
| 36 格箱子显示 3×12 | ✅ | Box_36 预制体 |
| 48 格箱子显示 4×12 | ✅ | Box_48 预制体 |

### 需求 4：物品拖拽转移

| 验收标准 | 状态 | 说明 |
|---------|------|------|
| 箱子 → 背包拖拽 | ❌ | Down 槽位未配置 |
| 背包 → 箱子拖拽 | ❌ | Down 槽位未配置 |
| 相同物品合并堆叠 | ⚠️ | 依赖槽位配置 |
| 不同物品交换 | ⚠️ | 依赖槽位配置 |

---

## 验收指南

### 前置条件

1. 确保 Box UI 预制体的 Down 区域已配置 `InventorySlotUI` 组件
2. 确保 StorageData 的 `boxUiPrefab` 已正确配置

### 测试步骤

#### 测试 1：打开箱子 UI

1. 在世界中放置一个箱子
2. 右键点击箱子
3. **预期**：
   - 箱子 UI 出现
   - Up 区域显示箱子内容
   - Down 区域显示背包内容（从索引 12 开始）
   - 控制台输出：`收集到 XX 个背包槽位`（XX > 0）

#### 测试 2：面板互斥

1. 打开箱子 UI
2. 按 Tab 键
3. **预期**：
   - 箱子 UI 关闭
   - 背包面板打开

#### 测试 3：物品转移

1. 打开箱子 UI
2. 从背包（Down）拖拽物品到箱子（Up）
3. **预期**：
   - 物品从背包移动到箱子
   - 两边 UI 同步更新

#### 测试 4：ESC 关闭

1. 打开箱子 UI
2. 按 ESC 键
3. **预期**：
   - 箱子 UI 关闭
   - 返回世界状态

---

## 相关文件

| 文件 | 说明 |
|------|------|
| `Assets/YYY_Scripts/UI/Box/BoxPanelUI.cs` | 箱子 UI 面板 |
| `Assets/YYY_Scripts/UI/Tabs/PackagePanelTabsUI.cs` | 背包面板切换管理 |
| `Assets/YYY_Scripts/World/Placeable/ChestController.cs` | 箱子控制器 |
| `Assets/222_Prefabs/UI/Box_12.prefab` | 12 格箱子 UI 预制体 |
| `Assets/222_Prefabs/UI/Box_24.prefab` | 24 格箱子 UI 预制体 |
| `Assets/222_Prefabs/UI/Box_36.prefab` | 36 格箱子 UI 预制体 |
| `Assets/222_Prefabs/UI/Box_48.prefab` | 48 格箱子 UI 预制体 |
