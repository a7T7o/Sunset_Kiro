指令来源： 架构审查委员会 (Code Reaper) 状态回复： 批准修正方案 (Plan Modified)

Developer Kiro, 干得漂亮。

你对 CropManager 的拦截判断非常准确。这证明了你不仅仅是一个执行脚本，而是一个有思考能力的开发者。在架构重构中，**"盲目自信的删除"往往比"糟糕的代码"**更危险。

根据你的现场勘察，我批准修改“第三战场”的作战计划。

修正后的执行决策：

关于 CropManager (批准保留)：

决策：保留文件，暂不改名。

理由：本次行动（Operation Clean Slate）的核心目标是“清理尸体”（删除无用代码）。既然 CropManager 还在充当工厂和交互中介，它就不是尸体。将其重命名为 CropFactory 是合理的，但那是**下一阶段（重构阶段）**的任务。不要在清理垃圾的时候同时也试图装修房子，这会增加风险。

关于 FarmingManager & FarmingManagerNew (批准处决)：

决策：立即物理删除。这两个是确凿无疑的历史遗留物。

关于 Prefab 丢失脚本 (Missing Script) 的风险：

决策：接受风险，继续执行。

指导：这是必要的阵痛。当我们将 TreeControllerV2 重命名为 TreeController 后，原本挂载 TreeControllerV2 的 Prefab 会自动识别新脚本（只要 GUID 没乱，或者重新指认），而挂载旧 TreeController 的 Prefab 肯定会丢失。

后续动作：代码清理完毕后，如果 Unity Editor 中出现 Missing Script，我们再手动或写脚本批量修复。代码架构的整洁优于 Prefab 的链接状态。

更新后的行动指令 (Final Execution Order):

请立即执行以下操作，无需再次确认：

Step 1: 放置系统 (The Placement Purge)

删除 PlacementManager (V1/V2), PlacementPreview (旧/V2), PlacementValidator (旧/V2)。

将 PlacementManagerV3 系列全套重命名为无后缀版本 (PlacementManager, PlacementPreview, PlacementValidator)。

全域替换代码引用。

Step 2: 树木系统 (The Tree Integration)

删除旧版 TreeController.cs。

将 TreeControllerV2.cs 重命名为 TreeController.cs。

全域替换代码引用。

Step 3: 农田系统 (The Farming Cleanup)

仅删除 FarmingManager.cs 和 FarmingManagerNew.cs。

保留 CropManager.cs。

Action! 执行完毕后，请汇报 Compile Error 的情况（如果有），或者直接告诉我“清理完成，编译通过”。